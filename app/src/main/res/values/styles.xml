<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="Theme.Bacon" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary">@color/theme_primary</item>
        <item name="android:colorPrimaryDark">@color/theme_primary_dark</item>
        <item name="android:colorAccent">@color/theme_accent</item>

    </style>

    <style name="Theme.Bacon.Detail">
        <item name="android:statusBarColor">@color/theme_accent</item>

    </style>

    <style name="CommuteCheckBox" parent="Theme.Bacon">
        <item name="colorControlNormal">@color/theme_primary_dark</item>   <!-- normal border color change as you wish -->
        <item name="colorControlActivated">@color/theme_primary_dark</item> <!-- activated color change as you wish -->
        <item name="android:textColor">@android:color/black</item> <!-- checkbox text color -->
    </style>

</resources>
